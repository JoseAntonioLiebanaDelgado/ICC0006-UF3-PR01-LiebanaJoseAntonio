import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ICC0006-UF3-PR01-LiebanaJoseAntonio',
  webDir: 'www'
};

export default config;
